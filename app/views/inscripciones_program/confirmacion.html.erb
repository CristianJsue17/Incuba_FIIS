<!-- app/views/inscripciones_program/confirmacion.html.erb -->

<!-- Font Awesome y Google Fonts -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Poppins:wght@400;500;600;700;800&display=swap" rel="stylesheet">

<% content_for :stylesheets do %>
<%= stylesheet_link_tag "home" %>
<% end %>

<% content_for :scripts do %>
  <%= javascript_include_tag "enhanced_header", defer: true %>
  <%= javascript_include_tag "footer", defer: true %>
<% end %>

<!-- Header -->
<header class="header" role="banner">
  <div class="header__container">
    <div class="header__logo">
      <a href="/" class="header__logo-link" aria-label="Ir al inicio">
        <div class="header__logo-container">
          <%= image_tag "logo-incuba.png", alt: "Logo Incuba UNAS", class: "header__logo-img" %>
        </div>
        <div class="header__text-container">
          <span class="header__logo-text">INCUBA UNAS</span>
        </div>
      </a>
    </div>

    <nav class="header__nav" role="navigation" aria-label="Navegación principal">
      <ul class="header__menu">
        <li class="header__menu-item header__menu-item--has-dropdown">
          <a href="#" class="header__menu-link"><%= t('navigation.programas') %></a>
          <div class="header__dropdown" role="menu">
            <ul class="header__dropdown-menu">
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Nuestros Programas</a>
              </li>
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Convocatorias</a>
              </li>
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Startup Culminados</a>
              </li>
            </ul>
          </div>
        </li>

        <li class="header__menu-item header__menu-item--has-dropdown">
          <%= link_to t('navigation.eventos'), eventos_path, class: "header__menu-link", "aria-haspopup" => "true", "aria-expanded" => "false" %>
          <div class="header__dropdown" role="menu">
            <ul class="header__dropdown-menu">
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Próximos Eventos</a>
              </li>
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Eventos Pasados</a>
              </li>
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Noticias</a>
              </li>
            </ul>
          </div>
        </li>

        <li class="header__menu-item header__menu-item--has-dropdown">
          <%= link_to t('navigation.servicios'), servicios_path, class: "header__menu-link", "aria-haspopup" => "true", "aria-expanded" => "false" %>
          <div class="header__dropdown" role="menu">
            <ul class="header__dropdown-menu">
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Preincubación</a>
              </li>
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Incubación</a>
              </li>
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Open Innovation</a>
              </li>
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Otros</a>
              </li>
            </ul>
          </div>
        </li>

        <li class="header__menu-item header__menu-item--has-dropdown">
          <%= link_to t('navigation.nosotros'), about_path, class: "header__menu-link", "aria-haspopup" => "true", "aria-expanded" => "false" %>
          <div class="header__dropdown" role="menu">
            <ul class="header__dropdown-menu">
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Nuestro Equipo</a>
              </li>
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Misión y Visión</a>
              </li>
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Nuestra Historia</a>
              </li>
            </ul>
          </div>
        </li>

        <li class="header__menu-item header__menu-item--has-dropdown">
          <%= link_to t('navigation.mentores'), mentores_path, class: "header__menu-link", "aria-haspopup" => "true", "aria-expanded" => "false" %>
          <div class="header__dropdown" role="menu">
            <ul class="header__dropdown-menu">
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">De la UNAS</a>
              </li>
              <li class="header__dropdown-item">
                <a href="#" class="header__dropdown-link" role="menuitem">Otras Instituciones</a>
              </li>
            </ul>
          </div>
        </li>

        <li class="header__menu-item">
          <%= link_to t('navigation.contacto'), contact_path, class: "header__menu-link" %>
        </li>
      </ul>
    </nav>

    <div class="header__actions">
      <%= render 'shared/language_switcher' %>
      
      <div class="header__login-container">
        <a href="<%= new_user_session_path %>"
           class="header__action-btn header__action-btn--login"
           data-turbo="false"
           aria-label="<%= t('navigation.iniciar_sesion') %>">
        </a>
        <span class="header__login-text"><%= t('navigation.iniciar_sesion') %></span>
      </div>

      <a href="<%= new_user_session_path %>"
         class="header__action-btn header__action-btn--mobile"
         data-turbo="false"
         aria-label="Iniciar sesión">
        <i class="fas fa-user" aria-hidden="true"></i>
      </a>
    </div>

    <button class="header__hamburger" aria-label="Abrir menú de navegación" aria-expanded="false">
      <span class="header__hamburger-line" aria-hidden="true"></span>
      <span class="header__hamburger-line" aria-hidden="true"></span>
      <span class="header__hamburger-line" aria-hidden="true"></span>
    </button>
  </div>
</header>

<!-- Decoración de fondo -->
<div class="background-decoration">
  <div class="bg-circle bg-circle-1"></div>
  <div class="bg-circle bg-circle-2"></div>
  <div class="bg-circle bg-circle-3"></div>
  <div class="bg-dots"></div>
  <div class="bg-line bg-line-1"></div>
  <div class="bg-line bg-line-2"></div>
</div>

<!-- Main Container -->
<div class="confirmation-container">
  <!-- Hero Section -->
  <div class="confirmation-hero">
    <div class="hero-content">
      <div class="success-animation">
        <div class="success-circle">
          <div class="success-checkmark">
            <div class="checkmark-circle"></div>
            <div class="checkmark-check"></div>
          </div>
        </div>
      </div>
      
      <div class="hero-text">
        <div class="hero-badge">
          <% if @tipo == 'preincubacion' %>
            <i class="fas fa-seedling"></i>
          <% elsif @tipo == 'incubacion' %>
            <i class="fas fa-rocket"></i>
          <% elsif @tipo == 'innovacion' %>
            <i class="fas fa-cogs"></i>
          <% else %>
            <i class="fas fa-check-circle"></i>
          <% end %>
          <span>Inscripción Completada</span>
        </div>
        
        <h1 class="hero-title">¡Inscripción Exitosa!</h1>
        <p class="hero-description">
          Tu solicitud para el programa de 
          <% if @tipo == 'preincubacion' %>
            <strong>Preincubación</strong>
          <% elsif @tipo == 'incubacion' %>
            <strong>Incubación</strong>
          <% elsif @tipo == 'innovacion' %>
            <strong>Innovación</strong>
          <% else %>
            <strong>Emprendimiento</strong>
          <% end %>
          ha sido enviada correctamente. Te contactaremos pronto con los siguientes pasos.
        </p>
      </div>
    </div>
  </div>

  <!-- Progress Section -->
  <div class="progress-section">
    <div class="progress-wrapper">
      <div class="progress-bar">
        <div class="progress-fill" style="width: 100%"></div>
      </div>
      <div class="progress-steps">
        <div class="step completed">
          <div class="step-circle">
            <i class="fas fa-user"></i>
          </div>
          <span class="step-label">Información Personal</span>
        </div>
        <div class="step completed">
          <div class="step-circle">
            <% if @tipo == 'preincubacion' %>
              <i class="fas fa-lightbulb"></i>
            <% elsif @tipo == 'incubacion' %>
              <i class="fas fa-rocket"></i>
            <% elsif @tipo == 'innovacion' %>
              <i class="fas fa-cogs"></i>
            <% else %>
              <i class="fas fa-check"></i>
            <% end %>
          </div>
          <span class="step-label">
            <% if @tipo == 'preincubacion' %>
              Emprendimiento
            <% elsif @tipo == 'incubacion' %>
              Proyecto
            <% elsif @tipo == 'innovacion' %>
              Innovación
            <% else %>
              Detalles
            <% end %>
          </span>
        </div>
        <div class="step active">
          <div class="step-circle">
            <i class="fas fa-check"></i>
          </div>
          <span class="step-label">Confirmación</span>
        </div>
      </div>
    </div>
  </div>

  <!-- Confirmation Card -->
  <div class="confirmation-card">
    <!-- Summary Section -->
    <div class="confirmation-section">
      <div class="section-header">
        <div class="section-icon success">
          <i class="fas fa-check-circle"></i>
        </div>
        <div class="section-title">
          <h2>Resumen de tu Inscripción</h2>
          <p>Hemos recibido toda tu información correctamente. Revisa los detalles a continuación.</p>
        </div>
      </div>
      
      <div class="summary-content">
        <div class="summary-item">
          <div class="summary-icon">
            <% if @tipo == 'preincubacion' %>
              <i class="fas fa-seedling"></i>
            <% elsif @tipo == 'incubacion' %>
              <i class="fas fa-rocket"></i>
            <% elsif @tipo == 'innovacion' %>
              <i class="fas fa-cogs"></i>
            <% else %>
              <i class="fas fa-graduation-cap"></i>
            <% end %>
          </div>
          <div class="summary-details">
            <h3>Programa Seleccionado</h3>
            <p>
              <% if @tipo == 'preincubacion' %>
                Programa de Preincubación
              <% elsif @tipo == 'incubacion' %>
                Programa de Incubación
              <% elsif @tipo == 'innovacion' %>
                Programa de Innovación
              <% else %>
                Programa de Emprendimiento
              <% end %>
            </p>
          </div>
        </div>
        
        <div class="summary-item">
          <div class="summary-icon">
            <i class="fas fa-calendar-check"></i>
          </div>
          <div class="summary-details">
            <h3>Fecha de Inscripción</h3>
            <p><%= Date.current.strftime("%d de %B de %Y") %></p>
          </div>
        </div>
        
        <div class="summary-item">
          <div class="summary-icon">
            <i class="fas fa-clock"></i>
          </div>
          <div class="summary-details">
            <h3>Estado</h3>
            <p>En revisión - Te contactaremos pronto</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Next Steps Section -->
    <div class="confirmation-section">
      <div class="section-header">
        <div class="section-icon">
          <i class="fas fa-route"></i>
        </div>
        <div class="section-title">
          <h2>Próximos Pasos</h2>
          <p>Esto es lo que puedes esperar después de tu inscripción.</p>
        </div>
      </div>
      
      <div class="steps-timeline">
        <div class="timeline-item">
          <div class="timeline-icon">
            <i class="fas fa-envelope"></i>
          </div>
          <div class="timeline-content">
            <h3>Confirmación por Email</h3>
            <p>Recibirás un correo electrónico de confirmación con todos los detalles de tu inscripción en los próximos minutos.</p>
            <span class="timeline-time">Inmediato</span>
          </div>
        </div>
        
        <div class="timeline-item">
          <div class="timeline-icon">
            <i class="fas fa-search"></i>
          </div>
          <div class="timeline-content">
            <h3>Revisión de Solicitud</h3>
            <p>Nuestro equipo revisará tu solicitud y evaluará si cumples con los requisitos del programa seleccionado.</p>
            <span class="timeline-time">2-3 días hábiles</span>
          </div>
        </div>
        
        <div class="timeline-item">
          <div class="timeline-icon">
            <i class="fas fa-phone"></i>
          </div>
          <div class="timeline-content">
            <h3>Contacto Personalizado</h3>
            <p>Te contactaremos para informarte sobre el resultado y coordinarte los siguientes pasos del proceso.</p>
            <span class="timeline-time">5-7 días hábiles</span>
          </div>
        </div>
        
        <div class="timeline-item">
          <div class="timeline-icon">
            <i class="fas fa-play-circle"></i>
          </div>
          <div class="timeline-content">
            <h3>Inicio del Programa</h3>
            <p>Si eres seleccionado, comenzarás tu journey emprendedor con nosotros y nuestro equipo de mentores.</p>
            <span class="timeline-time">Según cronograma</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Contact Information Section -->
    <div class="confirmation-section">
      <div class="section-header">
        <div class="section-icon">
          <i class="fas fa-headset"></i>
        </div>
        <div class="section-title">
          <h2>¿Necesitas Ayuda?</h2>
          <p>Nuestro equipo está disponible para resolver cualquier duda que tengas.</p>
        </div>
      </div>
      
      <div class="contact-grid">
        <div class="contact-item">
          <div class="contact-icon">
            <i class="fas fa-envelope"></i>
          </div>
          <div class="contact-details">
            <h3>Email</h3>
            <a href="mailto:contacto@incubaunas.edu.pe">contacto@incubaunas.edu.pe</a>
          </div>
        </div>
        
        <div class="contact-item">
          <div class="contact-icon">
            <i class="fas fa-phone"></i>
          </div>
          <div class="contact-details">
            <h3>Teléfono</h3>
            <a href="tel:+51999999999">(51) 999-999-999</a>
          </div>
        </div>
        
        <div class="contact-item">
          <div class="contact-icon">
            <i class="fas fa-map-marker-alt"></i>
          </div>
          <div class="contact-details">
            <h3>Ubicación</h3>
            <p>Av. Universitaria, Tingo María, Perú</p>
          </div>
        </div>
        
        <div class="contact-item">
          <div class="contact-icon">
            <i class="fas fa-clock"></i>
          </div>
          <div class="contact-details">
            <h3>Horario</h3>
            <p>Lunes a Viernes: 8:00 AM - 6:00 PM</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Action Buttons -->
    <div class="form-actions">
      <a href="<%= @tipo.present? ? programas_tipo_path(@tipo) : servicios_path %>" class="btn btn-secondary">
        <i class="fas fa-arrow-left"></i>
        <span>Volver a programas</span>
      </a>
      
      <a href="<%= root_path %>" class="btn btn-primary">
        <i class="fas fa-home"></i>
        <span>Ir al inicio</span>
      </a>
    </div>
  </div>
</div>

<!-- Footer -->
<footer class="footer">
    <div class="footer__decoration footer__decoration--top-left"></div>
    <div class="footer__decoration footer__decoration--bottom-right"></div>

    <div class="footer__container">
        <div class="footer__column footer__column--main">
            <div class="footer__logo-container">
                <div class="footer__logo">
                    <span class="footer__logo-icon">I</span>NCUBA UNAS
                </div>
                <p class="footer__description">
                    Incuba UNAS es una incubadora de empresas que busca impulsar el desarrollo de
                    emprendimientos innovadores y sostenibles en la región.
                </p>
            </div>

            <form class="footer__subscribe">
                <input type="email" class="footer__input" placeholder="Enter email">
                <button type="submit" class="footer__button">
                    <i class="fas fa-arrow-right"></i>
                    Contacto
                </button>
            </form>

            <div class="footer__social">
                <a href="#" class="footer__social-link">
                    <i class='fab fa-instagram'></i>
                </a>
                <a href="#" class="footer__social-link">
                    <i class='fab fa-twitter'></i>
                </a>
                <a href="#" class="footer__social-link">
                    <i class='fab fa-facebook'></i>
                </a>
            </div>
        </div>

        <div class="footer__column">
            <h3 class="footer__heading">Principal</h3>
            <ul class="footer__links">
                <li class="footer__item"><a href="#" class="footer__link">Programas</a></li>
                <li class="footer__item"><a href="#" class="footer__link">Nosotros</a></li>
                <li class="footer__item"><a href="#" class="footer__link">Portafolio</a></li>
                <li class="footer__item"><a href="#" class="footer__link">Equipo</a></li>
                <li class="footer__item"><a href="#" class="footer__link">Servicios</a></li>
                <li class="footer__item"><a href="#" class="footer__link">Contacto</a></li>
            </ul>
        </div>

        <div class="footer__column">
            <h3 class="footer__heading">Nosotros</h3>
            <ul class="footer__links">
                <li class="footer__item"><a href="#" class="footer__link">Otros servicios</a></li>
                <li class="footer__item"><a href="#" class="footer__link">Tutores</a></li>
                <li class="footer__item"><a href="#" class="footer__link">Eventos</a></li>
                <li class="footer__item"><a href="#" class="footer__link">Pro Innova</a></li>
            </ul>
        </div>

        <div class="footer__column">
            <h3 class="footer__heading">Enlaces de Interés</h3>
            <ul class="footer__links">
                <li class="footer__item"><a href="#" class="footer__link">UNAS</a></li>
                <li class="footer__item"><a href="#" class="footer__link">PRO Innóvate</a></li>
                <li class="footer__item"><a href="#" class="footer__link">Ministerio de la Producción</a></li>
            </ul>
        </div>
    </div>
</footer>

<!-- CSS Placeholder -->
<style>
/* CSS para Vista de Confirmación Modernizada */

/* Reset y base */
* {
  box-sizing: border-box;
}

body, html {
  margin: 0;
  padding: 0;
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  background-color: #f8fafc;
  color: #0f172a;
  line-height: 1.6;
  overflow-x: hidden;
}

/* Elementos decorativos de fondo */
.background-decoration {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: -1;
  overflow: hidden;
  pointer-events: none;
}

.bg-circle {
  position: absolute;
  border-radius: 50%;
  opacity: 0.08;
  filter: blur(80px);
}

.bg-circle-1 {
  width: 400px;
  height: 400px;
  background: linear-gradient(135deg, #4CAF50, #0f172a);
  top: -200px;
  right: -100px;
}

.bg-circle-2 {
  width: 600px;
  height: 600px;
  background: linear-gradient(135deg, #2e8b57, #3b82f6);
  bottom: -300px;
  left: -300px;
  opacity: 0.05;
}

.bg-circle-3 {
  width: 300px;
  height: 300px;
  background: #4CAF50;
  top: 40%;
  left: 5%;
  opacity: 0.06;
}

.bg-dots {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: radial-gradient(#e2e8f0 1px, transparent 1px);
  background-size: 40px 40px;
  opacity: 0.4;
}

.bg-line {
  position: absolute;
  background: linear-gradient(90deg, transparent 0%, #e2e8f0 50%, transparent 100%);
  height: 1px;
  width: 100%;
  opacity: 0.5;
}

.bg-line-1 {
  top: 30%;
  transform: rotate(-1deg);
}

.bg-line-2 {
  bottom: 35%;
  transform: rotate(1deg);
}

/* Container principal */
.confirmation-container {
  max-width: 1000px;
  margin: 0 auto;
  padding: 3rem 1rem;
  margin-top: 240px;
  margin-bottom: 3rem;
  position: relative;
  z-index: 1;
}

/* Hero Section */
.confirmation-hero {
  background: linear-gradient(135deg, #4CAF50 0%, #2e8b57 100%);
  border-radius: 1.5rem;
  padding: 3rem;
  margin-bottom: 2rem;
  color: white;
  position: relative;
  overflow: hidden;
  box-shadow: 0 10px 25px rgba(76, 175, 80, 0.3);
}

.confirmation-hero::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.1'%3E%3Ccircle cx='30' cy='30' r='2'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
  opacity: 0.3;
}

.hero-content {
  position: relative;
  z-index: 2;
  display: flex;
  align-items: center;
  gap: 2rem;
}

/* Animación de éxito */
.success-animation {
  flex-shrink: 0;
}

.success-circle {
  width: 120px;
  height: 120px;
  position: relative;
}

.success-checkmark {
  width: 120px;
  height: 120px;
  border-radius: 50%;
  display: block;
  stroke-width: 3;
  stroke: #ffffff;
  stroke-miterlimit: 10;
  box-shadow: inset 0px 0px 0px #ffffff;
  position: relative;
  background: rgba(255, 255, 255, 0.2);
  border: 3px solid rgba(255, 255, 255, 0.3);
  backdrop-filter: blur(10px);
}

.checkmark-circle {
  stroke-dasharray: 166;
  stroke-dashoffset: 166;
  stroke-width: 3;
  stroke-miterlimit: 10;
  stroke: rgba(255, 255, 255, 0.6);
  fill: none;
  width: 100%;
  height: 100%;
  border-radius: 50%;
  position: absolute;
  top: 0;
  left: 0;
  border: 3px solid rgba(255, 255, 255, 0.3);
}

.checkmark-check {
  transform-origin: 50% 50%;
  stroke-dasharray: 48;
  stroke-dashoffset: 48;
  position: absolute;
  top: 50%;
  left: 50%;
  width: 40px;
  height: 40px;
  transform: translate(-50%, -50%);
}

.checkmark-check::before {
  content: '✓';
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0);
  font-size: 2.5rem;
  color: white;
  font-weight: bold;
  transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

.success-checkmark.animate .checkmark-check::before {
  transform: translate(-50%, -50%) scale(1);
}

.success-checkmark.animate .checkmark-circle {
  animation: stroke 0.6s cubic-bezier(0.65, 0, 0.45, 1) forwards;
}

@keyframes stroke {
  100% {
    stroke-dashoffset: 0;
  }
}

/* Hero text content */
.hero-text {
  flex: 1;
}

.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  background: rgba(255, 255, 255, 0.2);
  padding: 0.5rem 1rem;
  border-radius: 1rem;
  font-size: 0.875rem;
  font-weight: 600;
  margin-bottom: 1rem;
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.3);
}

.hero-title {
  font-family: 'Poppins', sans-serif;
  font-size: 2.5rem;
  font-weight: 700;
  margin: 0 0 1rem;
  line-height: 1.2;
}

.hero-description {
  font-size: 1.125rem;
  margin: 0;
  opacity: 0.9;
  line-height: 1.6;
}

.hero-description strong {
  font-weight: 700;
  background: rgba(255, 255, 255, 0.2);
  padding: 0.2rem 0.5rem;
  border-radius: 0.375rem;
}

/* Progress Section */
.progress-section {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 2rem;
  gap: 1.5rem;
}

.progress-wrapper {
  flex: 1;
  max-width: 600px;
}

.progress-bar {
  height: 6px;
  background-color: #f3f4f6;
  border-radius: 0.375rem;
  margin-bottom: 1rem;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #4CAF50, #2e8b57);
  border-radius: 0.375rem;
  transition: width 500ms cubic-bezier(0.4, 0, 0.2, 1);
}

.progress-steps {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.step {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  max-width: 120px;
}

.step-circle {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background-color: #f3f4f6;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 0.5rem;
  color: #64748b;
  transition: all 300ms cubic-bezier(0.4, 0, 0.2, 1);
  font-size: 0.875rem;
}

.step.completed .step-circle {
  background: linear-gradient(135deg, #4CAF50, #2e8b57);
  color: white;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
}

.step.active .step-circle {
  background: linear-gradient(135deg, #4CAF50, #2e8b57);
  color: white;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  animation: pulse 2s infinite;
}

.step-label {
  font-size: 0.8rem;
  color: #64748b;
  font-weight: 500;
  line-height: 1.3;
}

.step.completed .step-label,
.step.active .step-label {
  color: #0f172a;
  font-weight: 600;
}

@keyframes pulse {
  0% {
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 0 0 0 rgba(76, 175, 80, 0.7);
  }
  70% {
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 0 0 10px rgba(76, 175, 80, 0);
  }
  100% {
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 0 0 0 rgba(76, 175, 80, 0);
  }
}

/* Confirmation Card */
.confirmation-card {
  background: #ffffff;
  border-radius: 1.5rem;
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  border: 1px solid #e2e8f0;
}

/* Confirmation Sections */
.confirmation-section {
  padding: 2rem;
  border-bottom: 1px solid #e2e8f0;
  opacity: 0;
  transform: translateY(20px);
  transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

.confirmation-section:last-child {
  border-bottom: none;
}

.confirmation-section.animate-in {
  opacity: 1;
  transform: translateY(0);
}

.section-header {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  margin-bottom: 1.5rem;
}

.section-icon {
  width: 48px;
  height: 48px;
  border-radius: 0.75rem;
  background: linear-gradient(135deg, #4CAF50, #2e8b57);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: 1.25rem;
  flex-shrink: 0;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
}

.section-icon.success {
  background: linear-gradient(135deg, #4CAF50, #45a049);
}

.section-title h2 {
  margin: 0 0 0.25rem;
  font-family: 'Poppins', sans-serif;
  font-size: 1.5rem;
  font-weight: 600;
  color: #0f172a;
}

.section-title p {
  margin: 0;
  color: #475569;
  font-size: 0.9rem;
  line-height: 1.5;
}

/* Summary Content */
.summary-content {
  display: grid;
  gap: 1.5rem;
}

.summary-item {
  display: flex;
  align-items: center;
  gap: 1rem;
  padding: 1.5rem;
  background: #f9fafb;
  border-radius: 0.75rem;
  border: 1px solid #e2e8f0;
  transition: all 300ms cubic-bezier(0.4, 0, 0.2, 1);
}

.summary-item:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
}

.summary-icon {
  width: 48px;
  height: 48px;
  border-radius: 0.75rem;
  background: linear-gradient(135deg, #4CAF50, #2e8b57);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: 1.25rem;
  flex-shrink: 0;
}

.summary-details h3 {
  margin: 0 0 0.25rem;
  font-size: 1.1rem;
  font-weight: 600;
  color: #0f172a;
}

.summary-details p {
  margin: 0;
  color: #475569;
  font-size: 0.9rem;
}

/* Timeline Steps */
.steps-timeline {
  position: relative;
}

.steps-timeline::before {
  content: '';
  position: absolute;
  left: 24px;
  top: 0;
  bottom: 0;
  width: 2px;
  background: linear-gradient(to bottom, #4CAF50, #e2e8f0);
}

.timeline-item {
  position: relative;
  display: flex;
  align-items: flex-start;
  gap: 1.5rem;
  margin-bottom: 2rem;
  opacity: 0;
  transform: translateX(-20px);
  transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

.timeline-item:last-child {
  margin-bottom: 0;
}

.timeline-item.animate-in {
  opacity: 1;
  transform: translateX(0);
}

.timeline-icon {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  background: linear-gradient(135deg, #4CAF50, #2e8b57);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: 1.1rem;
  flex-shrink: 0;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  position: relative;
  z-index: 1;
}

.timeline-content {
  flex: 1;
  padding-top: 0.5rem;
}

.timeline-content h3 {
  margin: 0 0 0.5rem;
  font-size: 1.1rem;
  font-weight: 600;
  color: #0f172a;
}

.timeline-content p {
  margin: 0 0 0.5rem;
  color: #475569;
  line-height: 1.6;
}

.timeline-time {
  display: inline-block;
  background: #e2e8f0;
  color: #64748b;
  padding: 0.25rem 0.5rem;
  border-radius: 0.375rem;
  font-size: 0.75rem;
  font-weight: 500;
}

/* Contact Grid */
.contact-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1.5rem;
}

.contact-item {
  display: flex;
  align-items: center;
  gap: 1rem;
  padding: 1.5rem;
  background: #f9fafb;
  border-radius: 0.75rem;
  border: 1px solid #e2e8f0;
  transition: all 300ms cubic-bezier(0.4, 0, 0.2, 1);
}

.contact-item:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
}

.contact-icon {
  width: 40px;
  height: 40px;
  border-radius: 0.5rem;
  background: linear-gradient(135deg, #4CAF50, #2e8b57);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: 1rem;
  flex-shrink: 0;
}

.contact-details h3 {
  margin: 0 0 0.25rem;
  font-size: 0.9rem;
  font-weight: 600;
  color: #0f172a;
}

.contact-details p {
  margin: 0;
  color: #475569;
  font-size: 0.85rem;
}

.contact-details a {
  color: #4CAF50;
  text-decoration: none;
  font-weight: 500;
  transition: color 300ms cubic-bezier(0.4, 0, 0.2, 1);
}

.contact-details a:hover {
  color: #2e8b57;
  text-decoration: underline;
}

/* Form Actions */
.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 1rem;
  margin-top: 2rem;
  padding: 2rem;
  border-top: 1px solid #e2e8f0;
  background: #f9fafb;
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  padding: 0.875rem 2rem;
  border-radius: 1rem;
  font-weight: 600;
  font-size: 0.9rem;
  transition: all 300ms cubic-bezier(0.4, 0, 0.2, 1);
  cursor: pointer;
  border: 2px solid transparent;
  text-decoration: none;
  position: relative;
  overflow: hidden;
}

.btn-secondary {
  background: #f3f4f6;
  color: #475569;
  border-color: #e2e8f0;
}

.btn-secondary:hover {
  background: #f9fafb;
  color: #0f172a;
  transform: translateY(-1px);
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
}

.btn-primary {
  background: linear-gradient(135deg, #4CAF50, #2e8b57);
  color: white;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  position: relative;
}

.btn-primary::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, #2e8b57, #1e5631);
  opacity: 0;
  transition: opacity 300ms cubic-bezier(0.4, 0, 0.2, 1);
}

.btn-primary:hover::before {
  opacity: 1;
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
}

.btn span {
  position: relative;
  z-index: 1;
}

.btn i {
  transition: transform 300ms cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  z-index: 1;
}

.btn-primary:hover i {
  transform: translateX(2px);
}

.btn-secondary:hover i {
  transform: translateX(-2px);
}

/* Animaciones */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes fadeInLeft {
  from {
    opacity: 0;
    transform: translateX(-30px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

/* Aplicar animaciones */
.confirmation-hero {
  animation: fadeInUp 0.6s ease-out;
}

.progress-section {
  animation: fadeInLeft 0.6s ease-out 0.2s both;
}

.confirmation-section:nth-child(1) { transition-delay: 0.1s; }
.confirmation-section:nth-child(2) { transition-delay: 0.2s; }
.confirmation-section:nth-child(3) { transition-delay: 0.3s; }

.timeline-item:nth-child(1) { transition-delay: 0.1s; }
.timeline-item:nth-child(2) { transition-delay: 0.2s; }
.timeline-item:nth-child(3) { transition-delay: 0.3s; }
.timeline-item:nth-child(4) { transition-delay: 0.4s; }

/* Efectos hover mejorados */
.section-icon:hover {
  animation: pulse 1s ease-in-out infinite;
}

/* Estados de validación y microinteracciones */
.contact-item:hover .contact-icon {
  transform: scale(1.1);
}

.summary-item:hover .summary-icon {
  transform: scale(1.05);
}

/* Responsive Design */
@media (max-width: 768px) {
  .confirmation-container {
    padding: 1rem;
    margin-top: 200px;
  }

  .confirmation-hero {
    padding: 1.5rem;
  }

  .hero-content {
    flex-direction: column;
    text-align: center;
    gap: 1.5rem;
  }

  .hero-title {
    font-size: 2rem;
  }

  .success-circle {
    width: 80px;
    height: 80px;
  }

  .success-checkmark {
    width: 80px;
    height: 80px;
  }

  .checkmark-check::before {
    font-size: 1.8rem;
  }

  .confirmation-section {
    padding: 1.5rem;
  }

  .section-header {
    flex-direction: column;
    text-align: center;
    align-items: center;
  }

  .contact-grid {
    grid-template-columns: 1fr;
    gap: 1rem;
  }

  .form-actions {
    flex-direction: column;
    padding: 1.5rem;
  }

  .btn {
    width: 100%;
    justify-content: center;
  }

  .progress-steps {
    gap: 0.5rem;
  }

  .step-label {
    font-size: 0.7rem;
  }

  .timeline-item {
    gap: 1rem;
  }

  .timeline-icon {
    width: 40px;
    height: 40px;
    font-size: 1rem;
  }

  .steps-timeline::before {
    left: 20px;
  }
}

@media (max-width: 480px) {
  .confirmation-container {
    padding: 0.5rem;
  }

  .confirmation-hero {
    padding: 1rem;
  }

  .hero-title {
    font-size: 1.75rem;
  }

  .hero-description {
    font-size: 1rem;
  }

  .confirmation-section {
    padding: 1rem;
  }

  .section-icon {
    width: 40px;
    height: 40px;
    font-size: 1rem;
  }

  .section-title h2 {
    font-size: 1.25rem;
  }

  .summary-item {
    padding: 1rem;
  }

  .contact-item {
    padding: 1rem;
  }

  .progress-steps .step-label {
    display: none;
  }

  .step-circle {
    width: 32px;
    height: 32px;
    font-size: 0.75rem;
  }
}

/* Mejoras de accesibilidad */
.btn:focus {
  outline: 2px solid #4CAF50;
  outline-offset: 2px;
}

/* Estilos para elementos interactivos */
.timeline-item:hover .timeline-icon {
  transform: scale(1.1);
}

/* Estados de carga */
.btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
  transform: none !important;
}

.btn:disabled:hover {
  transform: none !important;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1) !important;
}

/* Toast de éxito */
.success-toast {
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.toast-icon {
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 50%;
  flex-shrink: 0;
}

.toast-content h4 {
  margin: 0 0 0.25rem;
  font-size: 0.9rem;
  font-weight: 600;
}

.toast-content p {
  margin: 0;
  font-size: 0.8rem;
  opacity: 0.9;
}
</style>

<!-- JavaScript -->
<script>
setTimeout(function() {
  // Función para crear confeti (versión original mejorada)
  function createConfetti() {
    const confettiContainer = document.createElement('div');
    confettiContainer.className = 'confetti-container';
    confettiContainer.style.cssText = `
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 1000;
      overflow: hidden;
    `;
    document.body.appendChild(confettiContainer);
    
    const colors = ['#f94144', '#f3722c', '#f8961e', '#f9c74f', '#90be6d', '#43aa8b', '#577590', '#4CAF50', '#2196F3', '#FF9800'];
    
    for (let i = 0; i < 100; i++) {
      const confetti = document.createElement('div');
      confetti.className = 'confetti';
      confetti.style.cssText = `
        position: absolute;
        width: 10px;
        height: 10px;
        top: -10px;
        left: ${Math.random() * 100}vw;
        border-radius: 0;
        opacity: 1;
        background-color: ${colors[Math.floor(Math.random() * colors.length)]};
        animation: confetti 5s ease-in-out infinite;
        animation-delay: ${Math.random() * 3}s;
      `;
      confettiContainer.appendChild(confetti);
    }
    
    // Eliminar confeti después de la animación
    setTimeout(() => {
      confettiContainer.remove();
    }, 8000);
  }

  // Agregar estilos de animación para confeti (versión original)
  const confettiStyles = document.createElement('style');
  confettiStyles.textContent = `
    @keyframes confetti {
      0% {
        transform: translateY(0) rotate(0);
        opacity: 1;
      }
      100% {
        transform: translateY(100vh) rotate(720deg);
        opacity: 0;
      }
    }
  `;
  document.head.appendChild(confettiStyles);

  // Animación de entrada
  function initializeAnimations() {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('animate-in');
        }
      });
    }, {
      threshold: 0.1
    });

    // Observar elementos para animaciones
    document.querySelectorAll('.confirmation-section, .timeline-item, .contact-item').forEach(el => {
      observer.observe(el);
    });
  }

  // Animación del checkmark
  function animateSuccessCheckmark() {
    const checkmark = document.querySelector('.success-checkmark');
    if (checkmark) {
      setTimeout(() => {
        checkmark.classList.add('animate');
      }, 500);
    }
  }

  // Animación de la barra de progreso
  function animateProgressBar() {
    const progressFill = document.querySelector('.progress-fill');
    if (progressFill) {
      setTimeout(() => {
        progressFill.style.transition = 'width 1.5s cubic-bezier(0.4, 0, 0.2, 1)';
        progressFill.style.width = '100%';
      }, 300);
    }
  }

  // Efecto parallax suave
  function initParallax() {
    window.addEventListener('scroll', () => {
      const scrolled = window.pageYOffset;
      const parallax = document.querySelectorAll('.bg-circle');
      const speed = 0.5;

      parallax.forEach((element, index) => {
        const yPos = -(scrolled * speed * (index + 1) * 0.3);
        element.style.transform = `translateY(${yPos}px)`;
      });
    });

    // Efecto parallax con movimiento del mouse
    window.addEventListener('mousemove', function(e) {
      const circles = document.querySelectorAll('.bg-circle');
      const moveX = (e.clientX - window.innerWidth / 2) / 50;
      const moveY = (e.clientY - window.innerHeight / 2) / 50;
      
      circles.forEach((circle, index) => {
        const factor = (index + 1) * 0.4;
        circle.style.transform = `translate(${moveX * factor}px, ${moveY * factor}px)`;
      });
    });
  }

  // Efectos de hover mejorados
  function initHoverEffects() {
    document.querySelectorAll('.timeline-item, .contact-item, .summary-item').forEach(item => {
      item.addEventListener('mouseenter', function() {
        this.style.transform = 'translateY(-5px)';
      });
      
      item.addEventListener('mouseleave', function() {
        this.style.transform = 'translateY(0)';
      });
    });
  }

  // Contador animado para números
  function animateNumbers() {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const element = entry.target;
          const finalNumber = parseInt(element.getAttribute('data-number'));
          let currentNumber = 0;
          const increment = finalNumber / 60; // 60 frames para 1 segundo
          
          const timer = setInterval(() => {
            currentNumber += increment;
            if (currentNumber >= finalNumber) {
              currentNumber = finalNumber;
              clearInterval(timer);
            }
            element.textContent = Math.floor(currentNumber);
          }, 16); // ~60fps
        }
      });
    });

    document.querySelectorAll('[data-number]').forEach(el => observer.observe(el));
  }

  // Mensaje de éxito toast
  function showSuccessToast() {
    const toast = document.createElement('div');
    toast.className = 'success-toast';
    toast.innerHTML = `
      <div class="toast-icon">
        <i class="fas fa-check-circle"></i>
      </div>
      <div class="toast-content">
        <h4>¡Inscripción exitosa!</h4>
        <p>Tu solicitud ha sido enviada correctamente</p>
      </div>
    `;
    
    toast.style.cssText = `
      position: fixed;
      top: 20px;
      right: 20px;
      background: linear-gradient(135deg, #4CAF50, #45a049);
      color: white;
      padding: 16px 20px;
      border-radius: 12px;
      box-shadow: 0 10px 25px rgba(76, 175, 80, 0.3);
      z-index: 1000;
      display: flex;
      align-items: center;
      gap: 12px;
      max-width: 350px;
      transform: translateX(400px);
      transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      border: 1px solid rgba(255, 255, 255, 0.2);
    `;

    // Estilos para los elementos internos del toast
    const toastIcon = toast.querySelector('.toast-icon');
    if (toastIcon) {
      toastIcon.style.cssText = `
        width: 24px;
        height: 24px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: rgba(255, 255, 255, 0.2);
        border-radius: 50%;
        flex-shrink: 0;
      `;
    }

    const toastContent = toast.querySelector('.toast-content');
    if (toastContent) {
      const h4 = toastContent.querySelector('h4');
      const p = toastContent.querySelector('p');
      
      if (h4) {
        h4.style.cssText = `
          margin: 0 0 0.25rem;
          font-size: 0.9rem;
          font-weight: 600;
        `;
      }
      
      if (p) {
        p.style.cssText = `
          margin: 0;
          font-size: 0.8rem;
          opacity: 0.9;
        `;
      }
    }

    document.body.appendChild(toast);
    
    setTimeout(() => {
      toast.style.transform = 'translateX(0)';
    }, 100);
    
    setTimeout(() => {
      toast.style.transform = 'translateX(400px)';
      setTimeout(() => toast.remove(), 400);
    }, 4000);
  }

  // Función para crear efectos de partículas adicionales
  function createCelebrationParticles() {
    const particleContainer = document.createElement('div');
    particleContainer.style.cssText = `
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 999;
      overflow: hidden;
    `;
    document.body.appendChild(particleContainer);

    // Crear partículas brillantes
    for (let i = 0; i < 20; i++) {
      const particle = document.createElement('div');
      particle.style.cssText = `
        position: absolute;
        width: 4px;
        height: 4px;
        background: #fff;
        border-radius: 50%;
        top: ${Math.random() * 100}vh;
        left: ${Math.random() * 100}vw;
        opacity: 0;
        animation: sparkle 2s ease-in-out infinite;
        animation-delay: ${Math.random() * 2}s;
        box-shadow: 0 0 6px #fff;
      `;
      particleContainer.appendChild(particle);
    }

    // Estilo para la animación de brillos
    const sparkleStyles = document.createElement('style');
    sparkleStyles.textContent = `
      @keyframes sparkle {
        0%, 100% { opacity: 0; transform: scale(0); }
        50% { opacity: 1; transform: scale(1); }
      }
    `;
    document.head.appendChild(sparkleStyles);

    // Remover después de 6 segundos
    setTimeout(() => {
      particleContainer.remove();
      sparkleStyles.remove();
    }, 6000);
  }

  // Inicializar todas las funciones
  animateSuccessCheckmark();
  animateProgressBar();
  createConfetti();
  createCelebrationParticles();
  initializeAnimations();
  initParallax();
  initHoverEffects();
  animateNumbers();

  // Mostrar toast después de un momento
  setTimeout(showSuccessToast, 1000);

  // Agregar efecto de ripple a los botones
  function addRippleEffect() {
    document.querySelectorAll('.btn').forEach(button => {
      button.addEventListener('click', function(e) {
        const ripple = document.createElement('span');
        const rect = this.getBoundingClientRect();
        const size = Math.max(rect.width, rect.height);
        const x = e.clientX - rect.left - size / 2;
        const y = e.clientY - rect.top - size / 2;
        
        ripple.style.cssText = `
          position: absolute;
          width: ${size}px;
          height: ${size}px;
          left: ${x}px;
          top: ${y}px;
          background: rgba(255, 255, 255, 0.3);
          border-radius: 50%;
          transform: scale(0);
          animation: ripple 0.6s ease-out;
          pointer-events: none;
        `;
        
        this.style.position = 'relative';
        this.style.overflow = 'hidden';
        this.appendChild(ripple);
        
        setTimeout(() => {
          ripple.remove();
        }, 600);
      });
    });

    // Agregar estilo para la animación de ripple
    const rippleStyles = document.createElement('style');
    rippleStyles.textContent = `
      @keyframes ripple {
        to {
          transform: scale(2);
          opacity: 0;
        }
      }
    `;
    document.head.appendChild(rippleStyles);
  }

  addRippleEffect();

}, 100);
</script>